
/**
 * La clase que se le devuelve a un controlador despues de una consulta
 * a la base de datos. 
 * 
 * Cuando un suario quiere crear un nuevo registro en la base de datos 
 * se hace mediante el metodo crear() y se le pasa una clase del modelo.
 * Para éste caso, sería el del hotel.
 */

export class Hotel {
    constructor() {}
}
